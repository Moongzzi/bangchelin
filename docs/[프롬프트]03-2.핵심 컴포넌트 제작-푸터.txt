당신은 React + TypeScript 기반 프로젝트의 시니어 프론트엔드 엔지니어이자 시안 기반 UI 컴포넌트 설계 어시스턴트입니다.

이번 작업은 첨부된 푸터 이미지 구현에만 집중해주세요.

첨부한 이미지는 Bangchelin Guide의 푸터 시안입니다.
상단 정책 링크 영역, 안내 문구 영역, 저작권/문의 링크 영역, SNS 바로가기 영역을 공통 구조로 보고,
모바일형 좁은 폭 레이아웃과 데스크탑형 넓은 폭 레이아웃 variation을 재사용 가능한 Footer 컴포넌트로 설계해주세요.
이미지에서 확정 가능한 정보와 구현을 위한 추정 정보를 구분해서 정리해주세요.

목표는 단순히 화면 하나를 하드코딩하는 것이 아니라,
이미지에 나타난 반복 패턴을 분석해서
재사용 가능한 Footer 컴포넌트 구조를 설계하고 구현하는 것입니다.

중요한 원칙:
- 시안 충실도를 우선하세요.
- 과도하게 범용적인 디자인 시스템을 먼저 만들지 마세요.
- 현재 푸터 시안에서 실제로 반복되는 패턴만 공통화하세요.
- 다만 화면 전용 하드코딩이 아니라, 이후 재사용 가능한 props 구조는 설계하세요.
- any 사용 금지
- React + TypeScript strict mode 기준으로 작성
- 접근성 고려
- 불필요한 라이브러리 추가 금지
- 설명보다 실제 코드 중심으로 작성
- 아직 시안에 없는 variant는 과도하게 만들지 마세요.

입력으로 제공되는 자료는 다음일 수 있습니다.
- 푸터 이미지 캡처
- Figma 링크
- 디자이너 설명
- 레이아웃/반응형 메모
- 컬러/토큰 정보

당신은 이 자료를 바탕으로:
1. 푸터에서 반복되는 UI 패턴을 분석하고
2. 어떤 단위를 공통 컴포넌트로 추출할지 판단하고
3. 현재 단계에서 필요한 최소 foundation/token만 정의하고
4. 실제 프로젝트에 넣을 수 있는 코드로 구현해야 합니다.

특히 이미지가 제공되면 반드시 아래를 구분해서 설명하세요.
- 이미지에서 명확히 확인 가능한 사실
- 텍스트 설명으로 보강된 사실
- 구현을 위해 합리적으로 추정한 사실

이 3가지를 섞어서 단정하지 말고 구분해서 작성하세요.

출력은 반드시 아래 순서로 작성하세요.
1. 제공된 푸터 시안 해석 요약
2. 반복 패턴 분석
3. 이번에 공통 컴포넌트로 추출할 대상과 선정 이유
4. 제안 컴포넌트 구조
5. 제안 파일 구조
6. 필요한 최소 token / foundation 정리
7. 각 컴포넌트 Props 설계
8. TypeScript 코드
9. CSS Modules 코드
10. 접근성 처리 설명
11. 사용 예시 / mock data
12. 추후 확장 포인트 정리

출력 규칙:
- 먼저 “무엇을 공통 컴포넌트로 볼 것인지” 판단부터 하세요.
- 입력 자료가 부족하면 추정한 부분과 확정된 부분을 구분해서 작성하세요.
- 캡처만 보고 확정하기 어려운 값은 임의 단정하지 말고, 합리적 추정이라고 명시하세요.
- 코드에서 하드코딩을 반복하지 마세요.
- 현재 구현에 필요한 범위까지만 token/foundation을 정의하세요.
- 실제 프로젝트에 바로 넣을 수 있는 수준으로 작성하세요.

이제 아래 입력 정보를 바탕으로 작업해주세요.

[프로젝트 기본 정보]
프로젝트명:
Bangchelin Guide

기술 스택:
React + TypeScript

스타일 방식:
CSS Modules

라우팅 환경:
미정

아이콘 처리 방식:
SVG 컴포넌트 또는 자체 아이콘 컴포넌트

접근성 요구 수준:
기본 웹 접근성 준수 + 키보드 포커스 고려

---

[작업 목표]
이번에 우선 구현할 컴포넌트:
Footer

추가 캡처 이미지:
- 이미지 1: 모바일 또는 좁은 폭 푸터
- 이미지 2: 데스크탑 또는 넓은 폭 푸터

디자이너 메모 또는 화면 설명:
- 배경은 밝은 베이지 톤
- 상단에는 정책 링크가 가로로 배치됨
- 특정 정책 링크는 강조 처리됨
- 중간에는 서비스 설명/운영 안내 문구가 있음
- 그 아래에는 저작권 문구와 개발자 문의 링크가 있음
- 하단에는 SNS 바로가기 텍스트와 원형 아이콘 버튼들이 있음
- 전체적으로 좌측 정렬 기반의 단정한 정보형 푸터 구조임

---

[레이아웃/반응형]
브레이크포인트 기준:
mobile < 768, desktop >= 768

최대 콘텐츠 너비:
1200px 추정

모바일 대응 방향:
- 전체 세로 흐름 유지
- 좌우 패딩은 비교적 작게 설정
- 정책 링크는 줄바꿈 가능성 고려
- SNS 아이콘은 하단에서 가로 배치

데스크탑 대응 방향:
- 전체 구조는 유지
- 더 넓은 좌우 패딩 및 max-width 적용
- 콘텐츠는 좌측 정렬 상태로 유지되고, 우측은 여백 영역으로 남을 수 있음

---

[디자인 토큰/스타일 참고]
컬러 토큰:
- background : surface/default
- sns icon button background : brand/primary
- sns icon : surface/overlay
- text : text/tertiary
- Selected text : text/primary
---

[콘텐츠 초안]
정책 링크 예시:
- 서비스 이용약관
- 개인정보 처리방침
- 쿠키 설정

안내 문구 예시:
- 카카오톡 오픈채팅 <방탈출 미식 협회> 운영 커뮤니티
- ※ 카카오톡 오픈채팅방 운영을 위한 안내 및 관리 지원 사이트입니다.

메타 정보 예시:
- © 2026 옥. All rights reserved.
- 개발자 문의 바로가기

SNS 섹션 제목 예시:
- <방탈출 미식 협회> SNS 바로가기

SNS 링크 예시:
- 카카오톡 채널
- 디스코드

---

[구현 제약]
반드시 props로 받을 것:
- policyLinks
- notices
- metaItems
- socialLinks
- className (선택)

선택적으로 props로 받을 것:
- maxWidth
- background
- emphasizedPolicyKey
- socialTitle
- showTopDivider
- showBottomDivider

하드코딩해도 되는 것:
- 임시 링크 URL
- 임시 SVG placeholder
- 임시 저작권 텍스트
- 임시 SNS aria-label

피해야 할 것:
- 뉴스레터 입력창 등 시안에 없는 요소 추가
- 과도한 variant 설계
- 외부 UI 라이브러리 의존
- 실제 외부 링크 연동까지 한 번에 구현
- 시안에 없는 다단 레이아웃 선구현

---

[원하는 출력 방식]
원하는 결과물:
- Footer 중심 컴포넌트 설계
- 필요한 최소 하위 컴포넌트 코드
- 최소 token/foundation 분리안
- CSS Modules 예시
- mock data 예시
- 사용 예시 2종
  1) 모바일형
  2) 데스크탑형

우선 구현 범위:
- Footer UI 렌더링 구조
- 반응형 분기 기초
- 접근성 속성 반영
- props 기반 데이터 주입 구조

이번 단계에서 제외:
- 실제 외부 링크 연결 검증
- 아이콘 애니메이션
- 다국어 처리
- CMS 연동